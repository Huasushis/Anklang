# Anklang 开发约定

本文件适用于本仓库的全部代码和文档，写给第一次接手这个项目、且没有读过任何其他提示词的人。
读完本文件、`README.md` 和 `docs/plan.md`，不需要额外背景就能开始写代码。阶段 1 的
yuantiji.ac 转发模式已经实现并作为生产默认；阶段 2 的本地检索主体、阶段 3 的来源接口与
示例来源也已经实现，但默认关闭，而且仓库不包含真实爬虫。代码在 `anklang/` 包，测试在
`tests/`；运行方式与各阶段的准确状态见 `README.md`。`docs/plan.md` 是从零设计时留下的
历史规划，其中已经标明哪些最初设想被当前实现取代。

## 1. 这是什么项目

Anklang 是 USTC 算法竞赛协会命题系统 **Urmotiv** 的配套服务，只做一件事：判断一道新题是不是
"原题"（已经在某个公开题库出现过的题目）。

Urmotiv 是一个私有的命题、审题、组题协作系统。它在用户提交题目前需要检查这道题是不是原题，
但它不想、也不应该自己维护一个大规模的公开题库副本、模型密钥或抓取代码——这既不是它的核心
职责，也会带来不必要的合规和安全风险。所以这件事被拆成一个完全独立的服务，就是 Anklang。

**Anklang 与 Urmotiv 是两个独立仓库、独立部署、独立数据库的服务，只通过带版本号的 HTTP 接口
通信。** Anklang 不读取、不连接 Urmotiv 的数据库；候选题的正文和 Anklang 自己的公开题库都不会
进入 Urmotiv。语言、框架、存储都可以和 Urmotiv 不一样（实际选型见 `docs/plan.md` 第 1 节：
Anklang 使用 Python 3.11+ 标准库，Urmotiv 使用 TypeScript）。Anklang 的 HTTP 服务由 Python
自带的 `http.server` 提供，数据存储使用自带的 `sqlite3`；部署服务器没有 pip/venv，因此当前
实现不依赖 FastAPI、Pydantic、numpy 等需要另外安装的包。除非部署条件被明确改变并完成服务器
验证，否则不要引入第三方运行时依赖。

## 2. 与 Urmotiv 的关系和契约

Urmotiv 已经实现了调用 Anklang 的客户端代码（在 Urmotiv 仓库的
`plugins/anklang/src/index.ts`），本仓库要实现的服务端必须满足这份代码定义的契约，这是
**已经写死、不能协商的接口**，不是双方将来再商量的草案。完整字段说明见 `docs/plan.md` 第 2.2
节，这里只列最容易踩坑的几点：

- 唯一需要实现的契约接口：`POST /api/v1/checks/similarity`。
- 请求体和响应体的 `apiVersion` 字段必须是字面量字符串 `"1"`。
- 响应体用严格模式校验（JSON Schema 术语叫 `.strict()`：只允许 schema 里列出的字段，
  **多一个字段都会导致整次检查被判定失败**）。实现时要经常对照 schema 检查有没有多塞了
  字段。
- 响应里的 `checkedAt` 必须是 UTC 时间、以字面量 `Z` 结尾的 ISO 8601 格式（例如
  `2026-07-26T00:00:00.000Z`）。Urmotiv 一侧的校验规则默认**不接受** `+08:00` 这种带时区
  偏移的写法，写错会导致每一次响应都校验失败。
- 响应里的 `contentHash` 必须原样回显请求里的值；`candidates` 最多 50 条；整个响应体不能
  超过 2MB。
- Urmotiv 只发送查重必需的信息（题目名称、类型、知识点标签、基础题面、内容摘要），**不会
  发送**作者身份、学号、邮箱、题解、测试数据或审核意见。不要假设将来会拿到更多字段。
- Urmotiv 一侧默认"检查失败就阻止提交"（`failureBehavior` 默认为 `block`）。因为 Anklang
  阶段 1 依赖的第三方服务（yuantiji.ac）没有 SLA（服务等级承诺），Anklang 应当尽量返回
  "降级但合法"的 200 响应（候选为空、`message` 说明情况），而不是让请求整体失败——只有
  请求本身不合法时才返回错误状态码。

不确定的地方，`docs/plan.md` 第 2.2 节有逐字段的完整契约搬运，实现前务必对照一遍。

## 3. 安全红线（不可违反）

1. **自己编写的公开题目爬虫代码不得推送到公开仓库。** 只有"使用或改编现成开源项目"的部分
   可以进公开仓库，并且必须先核对对方许可证、保留来源和版权声明（见 `docs/plan.md` 第 5 节的
   许可证合规要求）。自己从零写的抓取逻辑、账号池管理、代理池管理这类代码，如果要用，只能放在
   不进 Git 的私有位置。
2. **VJudge 或任何 OJ 的账号、密码、Cookie、IP 代理凭据等，只能放在 `.env`（本地）或部署环境的
   私有环境变量里，永远不进 Git、不进日志、不进错误上报、不进 Docker 镜像层。** `.env.example`
   只列字段名，不给真实值，这一点和 Urmotiv 仓库的约定完全一致。
3. **不把 Urmotiv 传来的题面（`basicStatement`）明文写进常规日志、错误堆栈、监控事件或第三方
   错误上报服务。** 调试日志只能记录 `contentHash`、`requestId`、耗时、候选数量这类不还原
   题面内容的信息。LLM 复核环节尤其要注意：不要把完整题面连同调用日志一起发给日志系统。
4. **不长期囤积超出检索索引所需的候选题正文。** Anklang 展示"来源 + 链接 + 相似度 + 简短说明"，
   完整正文交给用户跳转到原始来源阅读；对于自己抓取或索引的第三方内容，留存范围应服务于生成
   检索索引这个目的本身，不做超出这个目的的转存或对外提供下载。
5. **遵从上游参考项目 `is-my-problem-new` 的 MIT 许可证。** 复制或改编其代码时，在对应文件或
   目录保留 LICENSE 副本和版权声明（`Copyright (c) 2023 Ziqian Zhong`），不移除、不掩盖。
6. **对第三方服务的调用要克制，尤其是 yuantiji.ac。** 它是个人自费维护的公开服务，不是为
   服务器对服务器调用设计的稳定 API。必须有速率限制、退避重试上限和熔断（熔断：当发现
   持续失败或行为异常时主动停止继续调用一段时间，而不是无限重试），不能当作无限配额的免费
   后端使用。上线前是否需要联系维护者说明用途，见 `docs/plan.md` 第 2.4 节。
7. **不得为了显得专业而拼造新词或堆砌术语。** 需要用到专业术语时（例如"向量检索""余弦相似度"
   "重排""OCR"），第一次出现就用日常语言解释是什么、为什么需要它——这个约定和 Urmotiv 主仓库
   一致，`docs/plan.md` 已经按这个约定写，新增文档和代码注释也要保持。

## 4. 开发顺序建议

详细分阶段规划在 `docs/plan.md`，这里只给顺序结论：

1. **阶段 1：yuantiji.ac 转发模式，已实现且默认启用。** 它把请求交给现有公开检索服务，再把
   结果整理成 Urmotiv 约定的数据结构。继续修改时要优先保证契约、限速和失败降级不退步。
2. **阶段 2：本地检索主体，已实现但默认关闭。** 它把文字转换成数字列表，再比较这些数字找出
   相似题目，同时用字面重合补充搜索。当前还没有正式公开题库数据源，不能把“代码能运行”写成
   “已经可以替代线上转发模式”。
3. **阶段 3：来源接口、导入调度和本地示例已经实现。** “来源接口”是让不同题目来源按同一组函数
   提供数据的约定。仓库不包含真实爬虫；vjudge 来源继续推迟，不要提前实现大规模爬虫、账号池或
   代理池，原因和真正实施前的条件见 `docs/plan.md` 第 4.2 节。

后续工作应先保持阶段 1 稳定，再为阶段 2/3 补充经过许可的真实来源、规模测试和检索质量评估。

## 5. 测试要求

- **契约测试**：用固定 JSON 样例（fixture）校验 Anklang 的请求/响应严格符合 Urmotiv 的
  schema——字段名、类型、`apiVersion` 字面量、`contentHash` 正则、`candidates` 数量上限、
  响应体积上限都要覆盖。参考 Urmotiv 侧 `plugins/anklang/test/anklang.test.ts` 的测试口味：
  校验"不该发送的字段没有发送""响应内容摘要不匹配时的行为""相似度过滤"等。
- **鉴权测试**：无 token、错误 token、正确 token 三种情况都要覆盖。
- **超时与降级测试**：上游服务不可用或超时时，Anklang 返回的是"优雅降级的 200"，不能让
  Urmotiv 一侧收到无法解释的失败。
- **外部依赖必须可替换（mock）**：yuantiji.ac、LLM 复核 API、未来的本地检索引擎，测试环境
  不能要求真正联网调用第三方付费服务。如果需要真实核对第三方接口行为，用单独标记、默认跳过、
  需要人手动触发的集成测试，运行时克制调用频率。
- **不得在测试快照、fixture 或仓库任何位置提交真实题目原文、真实 vjudge/OJ 账号密码或真实
  API 密钥。** 测试用的题面样例必须是自己编写的、无版权问题的内容。
- 涉及许可证合规的改动（复用上游代码）需要在对应目录附上许可证副本和来源说明，并在提交前
  确认没有遗漏署名。

## 6. 文档索引

- `README.md`：项目定位和当前状态的简短说明。
- `docs/plan.md`：从零设计时的分阶段规划和决策记录。文档顶部说明了当前标准库实现与最初
  FastAPI/Pydantic 设想的差异；其中仍保留来源合规、测试清单和待人工决定事项作为背景。
